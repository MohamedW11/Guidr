import {useLocation} from "./_shared/router"; import {Shell,OpportunityCard,opportunities} from "./_shared"; import "./_group.css";
export function Saved(){const[,setLocation]=useLocation();return <Shell active="Saved"><div className="content"><div className="g-label">Your list</div><h1 style={{fontSize:36,margin:"7px 0"}}>Saved opportunities<span style={{color:"var(--g-red)"}}>.</span></h1><p style={{fontSize:12,color:"var(--g-muted)",marginBottom:28}}>Everything you’ve bookmarked, in one place, so nothing gets lost in a hundred tabs.</p><div className="grid">{opportunities.slice(0,3).map(o=><OpportunityCard key={o.id} item={o} saved onOpen={()=>setLocation("/guidr/OpportunityDetail")}/>)}</div></div></Shell>}